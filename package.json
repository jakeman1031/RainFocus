{
  "name": "rainfocus-ui-jakearciniega",
  "version": "1.0.0",
  "private": true,
  "description": "RainFocus UI Challenge — single-page Event setup guide built with Next.js + SCSS.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "postbuild": "node scripts/postbuild.mjs",
    "start": "next start",
    "serve:build": "http-server build -p 4173 -c-1",
    "sandbox": "npm run build && http-server build -p 3000 -a 0.0.0.0 -c-1",
    "package": "npm run build && node scripts/package.mjs"
  },
  "dependencies": {
    "next": "16.2.6",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "sass": "^1.81.0"
  }
}
